<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device through backup or transfer. The encrypted database would be useless
  elsewhere without the Keystore-held material, and a half-restored vault is worse than no vault:
  users should restore from a .securevault file they made deliberately.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
